import React from "react";
import { Layout } from "antd";

const { Header } = Layout;

export default () => {
  return (
    <Header className="header" style={{ padding: 0 }}>
      Tes
    </Header>
  );
};
